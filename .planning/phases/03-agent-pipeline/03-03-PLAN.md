---
phase: 03-agent-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/components/demands/demand-detail.tsx
  - apps/web/src/components/demands/agent-run-list.tsx
  - apps/web/src/components/demands/requirements-view.tsx
  - apps/web/src/components/demands/plan-view.tsx
  - apps/web/src/app/(dashboard)/demands/[demandId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Demand detail page shows Discovery output (functional/non-functional requirements, complexity, ambiguities) in a structured format, not raw JSON"
    - "Demand detail page shows Planning output (tasks, files, dependencies, execution order) in a structured format"
    - "Demand detail page lists all Agent Runs with tokens, cost, and duration for each run"
    - "Demand detail page shows accumulated total tokens and total cost across all runs"
    - "Agent run list auto-refreshes via polling while agents are active"
  artifacts:
    - path: "apps/web/src/components/demands/agent-run-list.tsx"
      provides: "Agent run history list component"
      min_lines: 40
    - path: "apps/web/src/components/demands/requirements-view.tsx"
      provides: "Structured Discovery output display"
      min_lines: 30
    - path: "apps/web/src/components/demands/plan-view.tsx"
      provides: "Structured Planning output display"
      min_lines: 30
    - path: "apps/web/src/components/demands/demand-detail.tsx"
      provides: "Enhanced demand detail with agent outputs and run list"
  key_links:
    - from: "apps/web/src/app/(dashboard)/demands/[demandId]/page.tsx"
      to: "/api/agent-runs"
      via: "TanStack Query fetch for agent run list"
      pattern: "api/agent-runs"
    - from: "apps/web/src/components/demands/demand-detail.tsx"
      to: "apps/web/src/components/demands/requirements-view.tsx"
      via: "conditional render when demand.requirements is non-null"
      pattern: "RequirementsView"
    - from: "apps/web/src/components/demands/demand-detail.tsx"
      to: "apps/web/src/components/demands/plan-view.tsx"
      via: "conditional render when demand.plan is non-null"
      pattern: "PlanView"
    - from: "apps/web/src/components/demands/demand-detail.tsx"
      to: "apps/web/src/components/demands/agent-run-list.tsx"
      via: "always rendered in demand detail"
      pattern: "AgentRunList"
---

<objective>
Enhance the demand detail page to display agent execution outputs in a structured, readable format and show the full history of agent runs with their token/cost/duration metrics.

Purpose: Gives humans full visibility into what the AI agents produced and how much each execution cost -- the "human having total visibility via dashboard" part of the core value proposition.
Output: Three new components (RequirementsView, PlanView, AgentRunList) integrated into the existing demand detail page.
</objective>

<execution_context>
@C:/Users/igor_/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/igor_/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-pipeline/03-01-SUMMARY.md
@apps/web/src/components/demands/demand-detail.tsx
@apps/web/src/app/(dashboard)/demands/[demandId]/page.tsx
@apps/web/src/lib/api.ts
@packages/shared/src/types/index.ts
@packages/shared/src/schemas/agent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RequirementsView and PlanView structured display components</name>
  <files>
    apps/web/src/components/demands/requirements-view.tsx
    apps/web/src/components/demands/plan-view.tsx
  </files>
  <action>
    1. Create `apps/web/src/components/demands/requirements-view.tsx`:
       - "use client" directive
       - Import Badge from shadcn, import `DiscoveryOutput` type from `@techteam/shared`
       - Accept props: `{ requirements: DiscoveryOutput }`
       - Render structured sections:
         a. **Functional Requirements** -- numbered list/cards, each showing: id, description, acceptance criteria (in a muted text block)
         b. **Non-Functional Requirements** -- grouped by category, each showing: category badge (colored by type: performance=blue, security=red, usability=green, reliability=orange, maintainability=purple), description
         c. **Complexity** -- large badge with complexity value (S/M/L/XL) and a label explaining what it means (S=Small/trivial, M=Medium/few days, L=Large/week+, XL=Extra Large/major effort)
         d. **Ambiguities** (only if array is non-empty) -- warning-styled section (yellow/amber border), each ambiguity showing: question in bold, context in muted text. Include an alert icon and a message "This demand is paused until these questions are resolved."
         e. **Summary** -- paragraph with the discovery summary text
       - Use Tailwind classes consistent with the existing demand-detail.tsx style (rounded-lg border, text-sm, etc.)

    2. Create `apps/web/src/components/demands/plan-view.tsx`:
       - "use client" directive
       - Import Badge from shadcn, import `PlanningOutput` type from `@techteam/shared`
       - Accept props: `{ plan: PlanningOutput }`
       - Render structured sections:
         a. **Tasks** -- ordered list matching `plan.executionOrder`. For each task: title (bold), description, type badge (create=green, modify=blue, delete=red, test=purple, config=gray), complexity badge (trivial/simple/moderate/complex), files list (each showing path + action badge), dependencies list (task IDs this depends on, show as small badges)
         b. **Execution Order** -- visual flow showing task IDs in sequence (horizontal badges connected by arrows or a simple numbered list)
         c. **Risk Areas** (only if non-empty) -- amber-bordered section with bullet list of risk strings
         d. **Summary** -- paragraph with the planning summary text
       - Use consistent Tailwind styling

    Both components follow the existing pattern in demand-detail.tsx: section heading as `text-sm font-medium text-muted-foreground`, content in `rounded-lg border p-4` containers.
  </action>
  <verify>
    Run `cd apps/web && npx tsc --noEmit` -- should compile without errors.
    Verify both components export correctly and accept the expected props.
  </verify>
  <done>
    RequirementsView renders Discovery output as structured sections (functional reqs, non-functional reqs, complexity, ambiguities, summary) instead of raw JSON. PlanView renders Planning output as structured sections (tasks with files/dependencies, execution order, risk areas, summary). Both follow existing design patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: AgentRunList component and demand detail page integration</name>
  <files>
    apps/web/src/components/demands/agent-run-list.tsx
    apps/web/src/components/demands/demand-detail.tsx
    apps/web/src/app/(dashboard)/demands/[demandId]/page.tsx
  </files>
  <action>
    1. Create `apps/web/src/components/demands/agent-run-list.tsx`:
       - "use client" directive
       - Import `useQuery` from `@tanstack/react-query`, `api` from `@/lib/api`
       - Import Badge from shadcn, Clock, Zap, DollarSign, AlertCircle icons from lucide-react
       - Import `AgentRun` type from `@techteam/shared`
       - Accept props: `{ demandId: string, isAgentActive?: boolean }`
       - Use TanStack Query to fetch: `api.get<{ agentRuns: AgentRun[] }>('/api/agent-runs?demandId=' + demandId)`
         - queryKey: `['agent-runs', demandId]`
         - refetchInterval: `isAgentActive ? 5000 : false` -- poll every 5s while agents are active (matches board polling pattern), stop when inactive
       - Render a list of agent runs, each showing:
         a. **Phase** badge (discovery, planning) -- colored appropriately
         b. **Status** badge (queued=gray, running=blue+pulse, completed=green, failed=red, timeout=orange, paused=yellow)
         c. **Tokens**: input tokens + output tokens (with Zap icon)
         d. **Cost**: formatted as $X.XXXX USD (with DollarSign icon)
         e. **Duration**: formatted as Xs or Xm Ys (with Clock icon)
         f. **Attempt**: show attempt number if > 1 (e.g., "Attempt 2/3")
         g. **Error** (if status is failed/timeout): collapsible error message in red text
         h. **Timestamp**: relative time or formatted date
       - If no runs exist, show "No agent runs yet" in muted text
       - Sort by createdAt descending (newest first, which the API already does)

    2. Update `apps/web/src/app/(dashboard)/demands/[demandId]/page.tsx`:
       - The page currently fetches a single demand. It should now also pass the demand data to allow AgentRunList to determine if agents are active.
       - Determine `isAgentActive` from `demand.agentStatus === 'queued' || demand.agentStatus === 'running'`
       - If agents are active, also set `refetchInterval: 5000` on the demand query itself (so the detail page auto-refreshes when agents are running)

    3. Update `apps/web/src/components/demands/demand-detail.tsx`:
       - Import `RequirementsView` from `./requirements-view`
       - Import `PlanView` from `./plan-view`
       - Import `AgentRunList` from `./agent-run-list`
       - Import `DiscoveryOutput`, `PlanningOutput` from `@techteam/shared`
       - Accept additional props: `isAgentActive?: boolean`
       - Replace the existing raw JSON `<pre>` blocks for requirements and plan:
         - Requirements section: instead of `JSON.stringify(demand.requirements, null, 2)` in a `<pre>`, render `<RequirementsView requirements={demand.requirements as DiscoveryOutput} />`. Keep the conditional render (only when `demand.requirements != null`).
         - Plan section: instead of `JSON.stringify(demand.plan, null, 2)` in a `<pre>`, render `<PlanView plan={demand.plan as PlanningOutput} />`. Keep the conditional render (only when `demand.plan != null`).
       - Add an **Agent Status** indicator near the top (after the current stage display):
         - If `demand.agentStatus` is non-null, show a badge: running=blue with pulse animation, queued=gray, paused=yellow with "Paused - needs human input" text, failed=red
       - Add the AgentRunList component at the bottom of the detail page:
         ```tsx
         <div className="space-y-2">
           <h2 className="text-sm font-medium text-muted-foreground">
             Agent Runs
           </h2>
           <AgentRunList demandId={demand.id} isAgentActive={isAgentActive} />
         </div>
         ```
       - Keep the existing cost section (totalTokens, totalCostUsd) -- it already displays accumulated totals from the Demand model (DEM-06)
  </action>
  <verify>
    Run `cd apps/web && npx tsc --noEmit` -- should compile without errors.
    Start the web app: `cd apps/web && pnpm dev`. Navigate to a demand detail page. Verify:
    - The "Agent Runs" section appears (showing "No agent runs yet" for existing demands)
    - Requirements and Plan sections are ready to render structured output (will be populated when agents run)
    - No visual regressions from existing demand detail functionality
  </verify>
  <done>
    Demand detail page now: (1) shows Discovery output as structured requirements instead of raw JSON (DEM-04), (2) shows Planning output as structured task plan instead of raw JSON (DEM-04), (3) lists all Agent Runs with tokens, cost, and duration (DEM-05), (4) shows accumulated totalTokens and totalCostUsd (DEM-06 -- was already present, preserved), (5) auto-refreshes when agents are active via polling.
  </done>
</task>

</tasks>

<verification>
1. RequirementsView renders functional/non-functional requirements, complexity, ambiguities, and summary
2. PlanView renders tasks with files/dependencies, execution order, risk areas, and summary
3. AgentRunList fetches from /api/agent-runs and displays run history with metrics
4. AgentRunList polls every 5s when agents are active, stops when inactive
5. Demand detail page integrates all three new components
6. Agent status badge displays current agent state (running/queued/paused/failed)
7. Existing demand detail functionality (pipeline progress, metadata, cost) is preserved
</verification>

<success_criteria>
- Discovery output renders as structured sections (not raw JSON pre blocks)
- Planning output renders as structured sections (not raw JSON pre blocks)
- Agent run list shows phase, status, tokens, cost, duration for each run
- Agent status indicator shows when agents are running/paused/failed
- Auto-refresh polling works when agents are active
- All TypeScript compiles without errors
- No visual regressions on existing demand detail page
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-pipeline/03-03-SUMMARY.md`
</output>
