---
phase: 04-dev-testing
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/web/src/components/demands/development-view.tsx
  - apps/web/src/components/demands/testing-report-view.tsx
  - apps/web/src/components/demands/demand-detail.tsx
  - apps/web/src/app/(dashboard)/demands/[demandId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Demand detail page shows branch name and PR link when populated by the development agent"
    - "Demand detail page shows testing verdict, test results, code quality issues, and rejection reasons when populated by the testing agent"
    - "Demand detail page auto-refreshes when demand is in development or testing stage with an active agent"
    - "Rejection count and feedback are visible when a demand has been rejected"
  artifacts:
    - path: "apps/web/src/components/demands/development-view.tsx"
      provides: "Component displaying development agent output (branch, PR, commit summary, files changed)"
      min_lines: 40
    - path: "apps/web/src/components/demands/testing-report-view.tsx"
      provides: "Component displaying testing agent report (verdict, tests, quality issues, rejection reasons)"
      min_lines: 60
  key_links:
    - from: "apps/web/src/components/demands/demand-detail.tsx"
      to: "apps/web/src/components/demands/development-view.tsx"
      via: "conditional render when branchName or prUrl exists"
      pattern: "DevelopmentView"
    - from: "apps/web/src/components/demands/demand-detail.tsx"
      to: "apps/web/src/components/demands/testing-report-view.tsx"
      via: "conditional render when testingFeedback exists"
      pattern: "TestingReportView"
---

<objective>
Create UI components to display development agent output (branch, PR link, commit summary) and testing agent reports (verdict, test results, code quality issues, rejection reasons) in the demand detail page.

Purpose: Give users full visibility into what the development and testing agents produced. Without these components, development/testing output would be invisible -- the user would only see the demand moving between stages without understanding what happened.

Output: development-view.tsx, testing-report-view.tsx, updated demand-detail.tsx with new sections, updated polling for development/testing stages.
</objective>

<execution_context>
@C:/Users/igor_/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/igor_/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dev-testing/04-01-SUMMARY.md
@.planning/phases/03-agent-pipeline/03-03-SUMMARY.md

Key existing files to read before modifying:
@apps/web/src/components/demands/demand-detail.tsx
@apps/web/src/components/demands/requirements-view.tsx
@apps/web/src/components/demands/plan-view.tsx
@apps/web/src/app/(dashboard)/demands/[demandId]/page.tsx
@packages/shared/src/schemas/agent.ts
@packages/shared/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DevelopmentView and TestingReportView components</name>
  <files>
    apps/web/src/components/demands/development-view.tsx
    apps/web/src/components/demands/testing-report-view.tsx
  </files>
  <action>
Follow the exact same patterns established by requirements-view.tsx and plan-view.tsx from Phase 3 Plan 03 -- use shadcn/ui Badge, Card components, Tailwind styling, TypeScript typed props.

**1. Create `apps/web/src/components/demands/development-view.tsx`:**

Props: `{ branchName: string | null; prUrl: string | null; developmentOutput: DevelopmentOutput | null; rejectionCount: number }`

Import `DevelopmentOutput` from `@techteam/shared`.

Layout (only rendered when branchName or prUrl is populated):
- Section header: "Development" with a code icon or GitBranch icon (use lucide-react: `GitBranch`, `ExternalLink`)
- **Branch:** Show `branchName` in a monospace Badge (e.g., `<Badge variant="outline" className="font-mono text-xs">{branchName}</Badge>`)
- **Pull Request:** Show `prUrl` as a clickable link with ExternalLink icon. Use `<a href={prUrl} target="_blank" rel="noopener noreferrer">`. If prUrl is null, show "PR not yet created" in muted text.
- **Rejection count:** If rejectionCount > 0, show an amber-bordered alert: "Rejected {rejectionCount} time(s)" with appropriate warning styling (similar to ambiguities warning in requirements-view.tsx).
- If `developmentOutput` is non-null (parsed DevelopmentOutput):
  - **Approach:** Show developmentOutput.approach as a paragraph
  - **Files Changed:** Bulleted list of developmentOutput.filesChanged (monospace)
  - **Commit Message:** Show developmentOutput.commitMessage in a quote block
  - **Notes:** If developmentOutput.notes exists, show it in an info box

**2. Create `apps/web/src/components/demands/testing-report-view.tsx`:**

Props: `{ testingOutput: TestingOutput | null; isLatestReport: boolean }`

Import `TestingOutput` from `@techteam/shared`.

Layout (only rendered when testingOutput is non-null):
- Section header: "Testing Report"
- **Verdict badge:** Large badge showing "Approved" (green) or "Rejected" (red): `<Badge variant={output.verdict === "approved" ? "default" : "destructive"} className="text-sm">{output.verdict}</Badge>`
- **Summary:** Show output.summary as a paragraph
- **Test Results section:**
  - If testsRan is true:
    - Badge: "Tests {testsPassed ? 'Passed' : 'Failed'}" in green/red
    - If testOutput exists, show in a pre/code block (max height with overflow scroll)
  - If testsRan is false:
    - Muted text: "No automated tests were executed"
- **Code Quality section:**
  - Two indicator badges: "Plan adherence: {adheresToPlan ? 'Yes' : 'No'}", "Requirements adherence: {adheresToRequirements ? 'Yes' : 'No'}" in green/red
  - If issues array is non-empty, render as a list:
    - Each issue: severity badge (color-coded: critical=red, major=orange, minor=yellow, suggestion=blue), file in monospace, description, and optional suggestion in italic
  - If issues array is empty: "No code quality issues found"
- **Rejection Reasons section (only if verdict is "rejected"):**
  - Red-bordered box listing each rejection reason as a bullet point
  - Use same amber/red border pattern as ambiguities in requirements-view.tsx
  </action>
  <verify>
`npx tsc --noEmit` passes from apps/web. Both components accept typed props matching DevelopmentOutput and TestingOutput from @techteam/shared. Components use shadcn/ui Badge and existing Tailwind patterns.
  </verify>
  <done>
DevelopmentView renders branch name (monospace badge), PR link (clickable external link), rejection count warning, approach description, files changed list, commit message, and notes. TestingReportView renders verdict badge (green/red), summary, test results (ran/passed/output), code quality (plan/requirements adherence, issues by severity), and rejection reasons. Both use shadcn/ui Badge and match existing component patterns. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate into demand detail page with polling updates</name>
  <files>
    apps/web/src/components/demands/demand-detail.tsx
    apps/web/src/app/(dashboard)/demands/[demandId]/page.tsx
  </files>
  <action>
**1. Update `apps/web/src/components/demands/demand-detail.tsx`:**

Import the new components:
```typescript
import { DevelopmentView } from "./development-view"
import { TestingReportView } from "./testing-report-view"
```

Import types from @techteam/shared: `DevelopmentOutput`, `TestingOutput`.

Add DevelopmentView section between PlanView and AgentRunList (or after PlanView, before testing sections). Render conditionally when `demand.branchName || demand.prUrl`:
```tsx
{(demand.branchName || demand.prUrl) && (
  <DevelopmentView
    branchName={demand.branchName}
    prUrl={demand.prUrl}
    developmentOutput={/* extract latest development AgentRun output and cast as DevelopmentOutput */ null}
    rejectionCount={demand.rejectionCount ?? 0}
  />
)}
```

For developmentOutput: Since the development agent's structured output is stored on the AgentRun (not directly on the Demand), and we don't have a separate field for it, pass `null` for now. The development agent's output is a summary -- the real output is the code it wrote. The important fields (branchName, prUrl) come from the Demand itself.

Add TestingReportView section after DevelopmentView. Render conditionally when `demand.testingFeedback`:
```tsx
{demand.testingFeedback && (
  <TestingReportView
    testingOutput={demand.testingFeedback as TestingOutput}
    isLatestReport={true}
  />
)}
```

Maintain the existing section order:
1. Title, metadata, description (existing)
2. Pipeline progress (existing)
3. RequirementsView (existing -- Phase 3)
4. PlanView (existing -- Phase 3)
5. DevelopmentView (NEW)
6. TestingReportView (NEW)
7. AgentRunList (existing -- Phase 3)

**2. Update `apps/web/src/app/(dashboard)/demands/[demandId]/page.tsx`:**

The existing refetchInterval callback checks for `agentStatus === 'queued' || agentStatus === 'running'`. This already works for development and testing stages since the worker sets agentStatus to "queued" when enqueuing jobs. No change needed for polling logic.

However, verify the existing callback and ensure it covers both stages. If the current implementation is:
```typescript
refetchInterval: (query) => {
  const status = query.state.data?.demand?.agentStatus
  return status === 'queued' || status === 'running' ? 5000 : false
}
```
This already works. No modification needed.

If for any reason the polling is not working for development/testing stages, ensure the refetchInterval callback checks agentStatus generically (not stage-specific).
  </action>
  <verify>
1. `npx tsc --noEmit` passes from apps/web
2. DevelopmentView appears in demand detail when branchName or prUrl exists
3. TestingReportView appears in demand detail when testingFeedback exists
4. Polling continues to work for development and testing stages (agentStatus-based, already generic)
5. Section order in demand detail is logical: discovery output -> plan -> development -> testing -> agent runs
  </verify>
  <done>
DevelopmentView and TestingReportView integrated into demand-detail.tsx in logical order after existing RequirementsView and PlanView sections. DevelopmentView shows branch/PR info and rejection count. TestingReportView shows testing agent's structured report with verdict, test results, and code quality analysis. Polling works for all stages via the existing agentStatus-based refetchInterval. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes from monorepo root with zero errors
2. All 4 files exist: development-view.tsx (new), testing-report-view.tsx (new), demand-detail.tsx (modified), page.tsx (verified)
3. Components use TypeScript types from @techteam/shared (DevelopmentOutput, TestingOutput)
4. Components follow existing patterns from requirements-view.tsx and plan-view.tsx
5. Demand detail page shows development and testing sections conditionally
</verification>

<success_criteria>
- Demand detail page displays branch name and PR link after development agent runs
- Demand detail page displays testing agent's verdict, test results, code quality issues, and rejection reasons
- Rejection count is visible as a warning when > 0
- All new components use TypeScript types from shared package
- Components match the visual style of existing RequirementsView and PlanView
- Auto-refresh polling works for demands in development and testing stages
</success_criteria>

<output>
After completion, create `.planning/phases/04-dev-testing/04-03-SUMMARY.md`
</output>
