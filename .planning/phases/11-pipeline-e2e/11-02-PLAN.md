---
phase: 11-pipeline-e2e
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - apps/api/src/queues/agent.worker.ts
  - apps/api/src/queues/merge.worker.ts
  - apps/api/src/lib/git.ts
  - apps/api/src/lib/github.ts
  - apps/api/src/agents/base-agent.ts
  - apps/api/src/agents/development.agent.ts
  - apps/api/src/agents/testing.agent.ts
autonomous: false

must_haves:
  truths:
    - "A demand flows from Inbox through Discovery, Planning, Development, Testing, Merge, and Done without manual intervention beyond initial submission and clarification"
    - "Development agent creates a real branch with code changes and a PR exists on GitHub"
    - "Testing agent produces a structured approval or rejection verdict"
    - "An approved PR is merged and the demand reaches stage done with completedAt set"
    - "If testing rejects, the demand returns to Development with feedback for retry"
  artifacts:
    - path: "apps/api/src/queues/agent.worker.ts"
      provides: "Working development and testing phase handlers with git auth"
      contains: "handleDevelopmentPhase"
    - path: "apps/api/src/queues/merge.worker.ts"
      provides: "Working merge worker with git auth"
      contains: "processMergeJob"
  key_links:
    - from: "apps/api/src/queues/agent.worker.ts"
      to: "apps/api/src/agents/development.agent.ts"
      via: "runDevelopmentAgent call"
      pattern: "runDevelopmentAgent"
    - from: "apps/api/src/queues/agent.worker.ts"
      to: "apps/api/src/agents/testing.agent.ts"
      via: "runTestingAgent call"
      pattern: "runTestingAgent"
    - from: "apps/api/src/queues/agent.worker.ts"
      to: "apps/api/src/queues/merge.queue.js"
      via: "mergeQueue.add on testing approval"
      pattern: "mergeQueue.add"
---

<objective>
Run the full pipeline end-to-end on a real repository: create a test GitHub repo, configure a project in TechTeam, submit a demand, and validate that it flows through all 7 stages (Inbox -> Discovery -> Planning -> Development -> Testing -> Merge -> Done). Fix any runtime bugs discovered during execution.

This is a validation-and-fix plan, not a pure implementation plan. The pipeline code already exists from Phases 3-5. The goal is to exercise every code path for the first time and fix whatever breaks. Bugs are expected in the Development, Testing, and Merge stages since they have never been run with real data.

Purpose: Proves the entire TechTeam platform works end-to-end -- the culmination of all 10 prior phases.

Output: A working pipeline run with evidence (demand in Done state, PR merged on GitHub, agent run records with costs/tokens).
</objective>

<execution_context>
@C:/Users/igor_/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/igor_/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pipeline-e2e/11-RESEARCH.md
@.planning/phases/11-pipeline-e2e/11-01-SUMMARY.md

@apps/api/src/queues/agent.worker.ts
@apps/api/src/queues/merge.worker.ts
@apps/api/src/lib/git.ts
@apps/api/src/lib/github.ts
@apps/api/src/agents/base-agent.ts
@apps/api/src/agents/agent-router.ts
@apps/api/src/agents/development.agent.ts
@apps/api/src/agents/testing.agent.ts
@apps/api/src/agents/merge-resolver.agent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test repository, configure project, and prepare E2E demand</name>
  <files>
    (no source files modified -- this is infrastructure setup)
  </files>
  <action>
    **Step 1: Create a test GitHub repository.**

    Use the `gh` CLI to create a public test repo:
    ```bash
    gh repo create beethetech/techteam-e2e-test --public --description "Disposable E2E test repo for TechTeam pipeline validation" --clone
    ```

    If `beethetech` org doesn't work, create under the user's account. The key requirement is: the GitHub token configured in TechTeam Settings must have push and PR permissions on this repo.

    **Step 2: Seed the test repo with a minimal Node.js project.**

    Inside the cloned repo, create:
    - `package.json` with name, version, main, scripts (a simple `"test": "node test.js"`)
    - `src/index.ts` with a simple export (e.g., `export function hello() { return "Hello from TechTeam E2E test" }`)
    - `test.js` with a simple assertion (e.g., `const assert = require("assert"); assert(true); console.log("Tests passed")`)
    - `tsconfig.json` with basic TS config
    - `README.md` with a one-liner

    Commit and push to `main`.

    **Step 3: Verify TechTeam services are running locally.**

    Check that API, worker, web, PostgreSQL, and Redis are running:
    ```bash
    # Check API health
    curl -s http://localhost:3010/health

    # Check Redis
    redis-cli -p 6380 ping
    ```

    If not running, start them with `pnpm dev` in the appropriate workspaces. The worker is critical -- it processes the BullMQ jobs.

    **Step 4: Configure project in TechTeam.**

    Using the web UI at http://localhost:3009, or via direct API calls:

    1. Ensure Settings page has a valid GitHub token (with `repo` scope) and Anthropic API key
    2. Create a new project with:
       - Name: "E2E Test"
       - Repo URL: The GitHub URL of the test repo (e.g., `https://github.com/beethetech/techteam-e2e-test`)
       - Repo Path: The absolute local path to the cloned test repo (e.g., the path where `gh repo clone` put it)
       - Default Branch: `main`
       - Tech Stack: `Node.js, TypeScript`
       - Max Concurrent Dev: `1`

    **Step 5: Create a demand with an extremely detailed, unambiguous description.**

    Create a demand on the "E2E Test" project with:
    - **Title:** "Add fibonacci utility function"
    - **Description:** (must be detailed enough to avoid discovery ambiguities)
      ```
      Add a utility function that calculates the nth Fibonacci number.

      Technical requirements:
      - Create a new file `src/utils/fibonacci.ts`
      - Export a function `fibonacci(n: number): number` that returns the nth Fibonacci number (0-indexed: fibonacci(0)=0, fibonacci(1)=1, fibonacci(2)=1, fibonacci(5)=5, fibonacci(10)=55)
      - Use an iterative approach (not recursive) for efficiency
      - Handle edge cases: n < 0 should return 0, n must be a non-negative integer
      - Re-export the function from `src/index.ts` so it is accessible from the package root

      Acceptance criteria:
      - `src/utils/fibonacci.ts` exists with the exported function
      - `src/index.ts` re-exports `fibonacci` from `./utils/fibonacci`
      - The function handles n=0, n=1, n=10 correctly
      - No TypeScript compilation errors
      ```

    This level of detail should prevent the Discovery agent from finding ambiguities.
  </action>
  <verify>
    1. `gh repo view beethetech/techteam-e2e-test` (or user's repo) shows the repo exists
    2. The test repo has `main` branch with `package.json`, `src/index.ts`, `tsconfig.json`
    3. `curl -s http://localhost:3010/health` returns success
    4. A project named "E2E Test" exists in TechTeam with correct settings
    5. A demand "Add fibonacci utility function" exists in the Inbox column
  </verify>
  <done>
    - Disposable test GitHub repo exists with minimal Node.js/TypeScript project
    - TechTeam services running locally (API, worker, web, DB, Redis)
    - Project configured with correct repoPath, repoUrl, defaultBranch
    - Settings has valid GitHub token and Anthropic API key
    - Demand created with detailed, unambiguous description ready for pipeline execution
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute full pipeline and validate all 7 stages</name>
  <files>
    apps/api/src/queues/agent.worker.ts
    apps/api/src/queues/merge.worker.ts
    apps/api/src/lib/git.ts
    apps/api/src/lib/github.ts
    apps/api/src/agents/base-agent.ts
    apps/api/src/agents/development.agent.ts
    apps/api/src/agents/testing.agent.ts
  </files>
  <action>
    Drag the "Add fibonacci utility function" demand from Inbox to Discovery on the Kanban board and observe the entire pipeline execute automatically. Monitor worker logs for progress. Fix any runtime bugs that surface during the run -- this is a test-and-fix task where bugs are expected because Development, Testing, and Merge stages have never been exercised with real data.

    **Pipeline stages to monitor (each takes 1-5 minutes, Development up to 30 min):**

    1. **Discovery:** Agent analyzes demand. Should find NO ambiguities (description is detailed). If paused, fill clarification form.
    2. **Planning:** Agent decomposes demand into tasks. Auto-advances to Development.
    3. **Development:** Agent creates branch, writes code in test repo, commits. Worker pushes (using token injection from Plan 11-01) and creates PR.
    4. **Testing:** Agent reviews PR, runs tests, produces verdict. If approved -> Merge. If rejected -> back to Development with feedback.
    5. **Merge:** Worker merges branch into main, pushes, closes PR, sets demand to Done.

    **If a stage fails**, diagnose from worker logs and fix the bug in the relevant source file. Common expected issues:
    - Agent SDK options mismatch: fix in base-agent.ts
    - Git push authentication: verify injectGitToken works (from Plan 11-01)
    - PR creation errors: check github.ts token passing
    - Output parsing failures: check agent output schema handling

    After any fix, re-start the worker and potentially re-trigger the failed stage by resetting the demand via API:
    ```
    PATCH /api/demands/:id/stage with { "stage": "<previous-stage>" }
    ```
    then dragging back to the target stage.
  </action>
  <verify>
    Human verifies these checks after the pipeline run completes:

    1. Open the E2E Test project board -- demand card is in the **Done** column
    2. Open demand detail page:
       - Requirements JSON is populated (from Discovery)
       - Plan JSON is populated (from Planning)
       - Branch name is set (format: `demand/{id}-*`)
       - PR URL points to a real GitHub PR
       - Testing report shows verdict (approved/rejected), summary, code quality
       - `completedAt` timestamp is set
       - `mergeStatus` is "merged"
    3. On GitHub: `gh pr list --repo <test-repo> --state closed` shows the merged PR
    4. In the test repo: `git log --oneline -5` shows the demand branch merged into main
    5. `src/utils/fibonacci.ts` exists in the test repo on the main branch

    **PIPE requirements checklist:**
    - [ ] PIPE-01: Demand flowed through all 7 stages
    - [ ] PIPE-02: Development agent created branch, pushed code, opened PR
    - [ ] PIPE-03: Testing agent produced verdict with rationale
    - [ ] PIPE-04: Approved PR merged automatically via merge queue
    - [ ] PIPE-05: If rejection occurred, feedback loop worked correctly
  </verify>
  <done>
    - Demand reached stage "done" with completedAt set and mergeStatus "merged"
    - GitHub shows a closed/merged PR on the test repository
    - All 5 PIPE requirements verified (or PIPE-05 verified if a rejection naturally occurred)
    - Any runtime bugs fixed and committed
  </done>
</task>

</tasks>

<verification>
**PIPE-01:** Demand exists in Done stage with completedAt set
**PIPE-02:** GitHub shows a branch named `demand/{id}-*` and a PR (open or closed) on the test repo
**PIPE-03:** AgentRun record for testing phase has non-null output with verdict field
**PIPE-04:** PR is closed on GitHub, default branch has the merged commits
**PIPE-05:** If rejection occurred during the run: demand.rejectionCount > 0 and demand.testingFeedback is populated

Verify via API:
```bash
# Check demand reached Done
curl -s http://localhost:3010/api/demands/<id> | jq '{stage, mergeStatus, completedAt, rejectionCount, branchName, prUrl}'

# Check agent runs
curl -s http://localhost:3010/api/demands/<id>/agent-runs | jq '.[].phase'
```
</verification>

<success_criteria>
- A real demand successfully flows from Inbox to Done on a real GitHub repository
- A branch exists on GitHub with code changes made by the Development agent
- A PR was created and subsequently merged/closed
- Testing agent produced a structured review with verdict
- All 5 PIPE requirements are verified
- The pipeline completes without manual intervention beyond initial submission (and optional clarification)
</success_criteria>

<output>
After completion, create `.planning/phases/11-pipeline-e2e/11-02-SUMMARY.md`
</output>
