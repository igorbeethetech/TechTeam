---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - docker-compose.yml
  - .env
  - .env.example
  - .gitignore
  - tsconfig.json
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/server.ts
  - apps/api/src/lib/config.ts
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/lib/utils.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/schemas/project.ts
  - packages/shared/src/types/index.ts
  - packages/shared/src/constants/index.ts
  - packages/database/package.json
  - packages/database/tsconfig.json
  - packages/database/prisma.config.ts
  - packages/database/prisma/schema.prisma
  - packages/database/src/client.ts
  - packages/database/src/index.ts
autonomous: true

must_haves:
  truths:
    - "docker compose up -d starts PostgreSQL 16 and Redis 7 without errors"
    - "pnpm install succeeds across all workspaces"
    - "pnpm build compiles all packages without TypeScript errors"
    - "Prisma generates client to packages/database/generated/prisma"
    - "Prisma migrations run against PostgreSQL (prisma migrate dev creates initial migration)"
    - "Fastify server starts on port 3001 and responds to GET /health with 200"
    - "Next.js dev server starts on port 3000 and renders a page"
    - "packages/shared exports Zod schemas importable by both apps/api and apps/web"
  artifacts:
    - path: "docker-compose.yml"
      provides: "PostgreSQL 16 + Redis 7 services"
      contains: "postgres:16-alpine"
    - path: "packages/database/prisma/schema.prisma"
      provides: "Database schema with Project model"
      contains: "model Project"
    - path: "packages/database/prisma.config.ts"
      provides: "Prisma 7 config with driver adapter setup"
      contains: "defineConfig"
    - path: "packages/database/src/client.ts"
      provides: "PrismaClient with PrismaPg adapter"
      contains: "PrismaPg"
    - path: "packages/shared/src/schemas/project.ts"
      provides: "Zod validation schemas for project"
      contains: "projectCreateSchema"
    - path: "apps/api/src/server.ts"
      provides: "Fastify server with CORS and health endpoint"
      contains: "Fastify"
    - path: "turbo.json"
      provides: "Turborepo task pipeline configuration"
      contains: "dependsOn"
  key_links:
    - from: "apps/api/src/server.ts"
      to: "packages/database"
      via: "workspace dependency import"
      pattern: "@techteam/database"
    - from: "apps/api/src/server.ts"
      to: "packages/shared"
      via: "workspace dependency import"
      pattern: "@techteam/shared"
    - from: "packages/database/src/client.ts"
      to: "packages/database/generated/prisma"
      via: "Prisma generated client import"
      pattern: "from.*generated/prisma"
---

<objective>
Scaffold the Turborepo monorepo with all workspace packages, Docker Compose for local services, Prisma 7 schema with database connection, Fastify server skeleton, Next.js app skeleton, and shared Zod schemas.

Purpose: Establish the foundational infrastructure that all subsequent plans (auth, tenant isolation, project CRUD) build upon. Without a working monorepo, database connection, and server skeleton, nothing else can proceed.

Output: A buildable monorepo where `docker compose up -d` starts services, `pnpm build` compiles everything, Prisma generates its client and runs migrations, Fastify responds on port 3001, and Next.js renders on port 3000.
</objective>

<execution_context>
@C:/Users/igor_/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/igor_/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monorepo structure, Docker Compose, and package scaffolding</name>
  <files>
    package.json
    pnpm-workspace.yaml
    turbo.json
    docker-compose.yml
    .env
    .env.example
    .gitignore
    tsconfig.json
    apps/api/package.json
    apps/api/tsconfig.json
    apps/web/package.json
    apps/web/tsconfig.json
    packages/shared/package.json
    packages/shared/tsconfig.json
    packages/database/package.json
    packages/database/tsconfig.json
  </files>
  <action>
    Create the full monorepo directory structure with 4 workspaces: apps/web, apps/api, packages/shared, packages/database.

    **Root files:**
    - `package.json`: name "techteam", private true, `"type": "module"`, `"packageManager": "pnpm@10.29.2"`, `"engines": { "node": ">=22.0.0" }`. Scripts: dev, build, lint, type-check, db:generate, db:migrate, db:seed, docker:up, docker:down, docker:reset (all delegating to turbo or docker compose). Use the exact script definitions from RESEARCH.md "Root package.json Scripts" section.
    - `pnpm-workspace.yaml`: packages list `["apps/*", "packages/*"]`
    - `turbo.json`: Use exact config from RESEARCH.md. Tasks: build (dependsOn ^build, outputs .next/**, dist/**, generated/**), dev (cache false, persistent true), db:generate, db:migrate, db:seed (all cache false), lint and type-check (dependsOn ^build).
    - `docker-compose.yml`: Use exact config from RESEARCH.md. PostgreSQL 16-alpine on port 5433:5432, Redis 7-alpine on port 6380:6379. Named volumes techteam_pgdata and techteam_redisdata. Healthcheck on PostgreSQL.
    - `.env`: Copy the values from RESEARCH.md .env.example. Generate a random 32+ char string for BETTER_AUTH_SECRET.
    - `.env.example`: Same as .env but with placeholder values (no real secrets).
    - `.gitignore`: node_modules, .next, dist, generated, .env (NOT .env.example), *.tsbuildinfo, .turbo
    - `tsconfig.json` (root): Base config with `"module": "ESNext"`, `"moduleResolution": "bundler"`, `"target": "ES2022"`, `"strict": true, "esModuleInterop": true, "skipLibCheck": true, "forceConsistentCasingInFileNames": true`. This is the base that workspace tsconfigs extend.

    **apps/api:**
    - `package.json`: name "@techteam/api", `"type": "module"`, dependencies on fastify, @fastify/cors, @fastify/cookie, dotenv. devDependencies on @types/node, tsx, typescript, prisma. Workspace dependencies: `"@techteam/database": "workspace:*"`, `"@techteam/shared": "workspace:*"`. Scripts: `"dev": "tsx watch src/server.ts"`, `"build": "tsc"`, `"start": "node dist/server.js"`.
    - `tsconfig.json`: Extends root tsconfig, compilerOptions `"outDir": "dist"`, `"rootDir": "src"`. Include `["src"]`.

    **apps/web:**
    - `package.json`: name "@techteam/web", `"type": "module"`. Dependencies on next, react, react-dom, @tanstack/react-query, zod. Workspace dependencies: `"@techteam/shared": "workspace:*"`. Scripts: `"dev": "next dev"`, `"build": "next build"`, `"start": "next start"`.
    - `tsconfig.json`: Standard Next.js 15 App Router tsconfig with path alias `"@/*": ["./src/*"]`.

    **packages/shared:**
    - `package.json`: name "@techteam/shared", `"type": "module"`, `"main": "src/index.ts"`, `"types": "src/index.ts"`. Dependencies on zod. Workspace protocol export for both apps.
    - `tsconfig.json`: Extends root, includes `["src"]`.

    **packages/database:**
    - `package.json`: name "@techteam/database", `"type": "module"`, `"main": "src/index.ts"`, `"types": "src/index.ts"`. Dependencies on @prisma/client, @prisma/adapter-pg, pg, dotenv. devDependencies on prisma, @types/pg. Scripts: `"db:generate": "prisma generate"`, `"db:migrate": "prisma migrate dev"`, `"db:seed": "tsx prisma/seed.ts"`.
    - `tsconfig.json`: Extends root, includes `["src", "prisma"]`.

    After creating all files, run `pnpm install` to install all dependencies. Do NOT use `pnpm dlx create-turbo` -- create everything manually to match the exact structure needed.
  </action>
  <verify>
    Run `pnpm install` -- must succeed without errors.
    Run `docker compose up -d` -- both postgres and redis containers must start (verify with `docker compose ps`).
    Run `docker compose down` after verification.
  </verify>
  <done>All workspace packages resolve, Docker Compose starts PostgreSQL 16 and Redis 7 cleanly, pnpm install completes without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Prisma 7 schema, database client, shared types, and server skeleton</name>
  <files>
    packages/database/prisma.config.ts
    packages/database/prisma/schema.prisma
    packages/database/src/client.ts
    packages/database/src/index.ts
    packages/shared/src/index.ts
    packages/shared/src/schemas/project.ts
    packages/shared/src/types/index.ts
    packages/shared/src/constants/index.ts
    apps/api/src/server.ts
    apps/api/src/lib/config.ts
    apps/web/next.config.ts
    apps/web/src/app/layout.tsx
    apps/web/src/app/page.tsx
    apps/web/src/lib/utils.ts
  </files>
  <action>
    **packages/database - Prisma 7 setup:**

    Create `prisma.config.ts` using the EXACT pattern from RESEARCH.md Pattern 1:
    ```typescript
    import "dotenv/config"
    import { defineConfig, env } from "prisma/config"
    export default defineConfig({
      schema: "prisma/schema.prisma",
      migrations: { path: "prisma/migrations" },
      datasource: { url: env("DATABASE_URL") },
    })
    ```

    Create `prisma/schema.prisma` using RESEARCH.md Pattern 5. Include:
    - Generator: `provider = "prisma-client"`, `output = "../generated/prisma"` (NOT prisma-client-js)
    - Datasource: `provider = "postgresql"` (NO url here -- it's in prisma.config.ts)
    - Better Auth will need tables later (user, session, account, verification, organization, member, invitation) -- for NOW, add a comment placeholder noting these will be added in Plan 02
    - Project model with all fields from RESEARCH.md: id (cuid), tenantId, name, description (optional), repoUrl, repoPath, defaultBranch (default "main"), techStack, status (ProjectStatus enum), maxConcurrentDev (default 1), mergeStrategy (MergeStrategy enum), createdAt, updatedAt. Index on tenantId.
    - ProjectStatus enum: active, archived
    - MergeStrategy enum: fifo, priority

    Create `src/client.ts` using RESEARCH.md Pattern 1:
    ```typescript
    import "dotenv/config"
    import { PrismaPg } from "@prisma/adapter-pg"
    import { PrismaClient } from "../generated/prisma/client.js"
    const connectionString = process.env.DATABASE_URL!
    const adapter = new PrismaPg({ connectionString })
    const prisma = new PrismaClient({ adapter })
    export { prisma, PrismaClient }
    ```

    Create `src/index.ts` that re-exports from client.ts: `export { prisma, PrismaClient } from "./client.js"`

    **packages/shared - Zod schemas and types:**

    Create `src/schemas/project.ts` using RESEARCH.md Pattern 4:
    - `projectCreateSchema`: name (string min 2 max 100), description (string max 500 optional), repoUrl (string url), repoPath (string min 1), techStack (string min 1), maxConcurrentDev (number int min 1 max 3 default 1), mergeStrategy (enum fifo/priority default fifo)
    - `projectUpdateSchema`: projectCreateSchema.partial()
    - Export inferred types: ProjectCreate, ProjectUpdate

    Create `src/types/index.ts`: Export type aliases for ProjectStatus ("active" | "archived"), MergeStrategy ("fifo" | "priority"). Export a Project interface matching the Prisma model shape (id, tenantId, name, description, repoUrl, repoPath, defaultBranch, techStack, status, maxConcurrentDev, mergeStrategy, createdAt, updatedAt).

    Create `src/constants/index.ts`: Export PROJECT_STATUS and MERGE_STRATEGY as const objects, PIPELINE_STAGES array with the 7 stage names (inbox, discovery, planning, development, testing, merge, done).

    Create `src/index.ts`: Re-export all from schemas/project, types/index, constants/index.

    **apps/api - Fastify server skeleton:**

    Create `src/lib/config.ts`: Load and validate environment variables (DATABASE_URL, REDIS_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL, WEB_URL, API_PORT). Export typed config object.

    Create `src/server.ts`: Basic Fastify server setup from RESEARCH.md "Fastify Server Setup" section but ONLY the skeleton for now (no auth routes, no project routes, no tenant plugin -- those come in Plans 02 and 03). Include:
    - Import Fastify, @fastify/cors, @fastify/cookie
    - Register CORS with origin from config (WEB_URL), credentials true
    - Register cookie plugin
    - Add GET /health route returning `{ status: "ok", timestamp: new Date().toISOString() }`
    - Listen on API_PORT (default 3001), host 0.0.0.0
    - Log startup message

    **apps/web - Next.js skeleton:**

    Create `next.config.ts`: Minimal Next.js 15 config. Add `transpilePackages: ["@techteam/shared"]` for workspace package support.

    Create `src/app/layout.tsx`: Root layout with html, body, basic metadata (title "TechTeam Platform").

    Create `src/app/page.tsx`: Simple landing page with "TechTeam Platform" heading and a status message. Nothing fancy -- this gets replaced by real UI in Plan 03.

    Create `src/lib/utils.ts`: The `cn()` helper function using clsx and tailwind-merge.

    **Initialize shadcn/ui** by running `npx shadcn@latest init` in apps/web (accept defaults for Tailwind v4, New York style). This sets up the Tailwind v4 config, CSS variables, and component infrastructure.

    **Run Prisma generate and migrate:**
    1. Start Docker: `docker compose up -d`
    2. Wait for PostgreSQL healthcheck to pass
    3. Run `pnpm --filter @techteam/database db:generate` (generates Prisma client)
    4. Run `pnpm --filter @techteam/database db:migrate` with migration name "init" (creates tables)
    5. Verify migration succeeded

    **Build everything:**
    Run `pnpm build` to verify the full monorepo compiles.
  </action>
  <verify>
    1. `docker compose up -d` -- PostgreSQL and Redis running (docker compose ps shows healthy)
    2. `pnpm --filter @techteam/database db:generate` -- generates client without errors
    3. `pnpm --filter @techteam/database db:migrate` -- creates migration and applies it
    4. `pnpm build` -- all workspaces compile without TypeScript errors
    5. Start Fastify: `pnpm --filter @techteam/api dev` -- server starts on port 3001
    6. `curl http://localhost:3001/health` -- returns `{"status":"ok","timestamp":"..."}`
    7. Start Next.js: `pnpm --filter @techteam/web dev` -- starts on port 3000
    8. Verify shared package import works: apps/api can import from @techteam/shared and @techteam/database
  </verify>
  <done>
    Prisma 7 generates client and runs migrations against PostgreSQL. Fastify server responds on port 3001 with /health endpoint. Next.js renders on port 3000. Shared Zod schemas are importable across workspaces. Full monorepo builds with `pnpm build`.
  </done>
</task>

</tasks>

<verification>
Phase infrastructure verified by:
1. `docker compose up -d && docker compose ps` -- both services running
2. `pnpm build` -- zero TypeScript errors across all workspaces
3. `curl http://localhost:3001/health` -- Fastify responds with 200
4. Prisma migration exists in packages/database/prisma/migrations/
5. Generated Prisma client exists in packages/database/generated/prisma/
6. packages/shared exports are importable from both apps
</verification>

<success_criteria>
- INFRA-01: Monorepo with apps/web, apps/api, packages/shared, packages/database builds with pnpm
- INFRA-02: Docker Compose starts PostgreSQL 16 and Redis 7 with one command
- INFRA-03: Prisma schema with migrations running against PostgreSQL
- INFRA-04: Zod schemas in packages/shared importable by both apps
- INFRA-05: Fastify server responds with JSON on /health (full routes added in Plans 02-03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
